/*
 * SecureVision Monochromatic Stylesheet
 *
 * Strict Design Philosophy:
 * - Zero border-radius on ALL elements (sharp edges only)
 * - Light theme defaults: white backgrounds, black primary actions
 * - Subtle monochrome borders for unfocused states
 * - No gradients, shadows, or bevels (completely flat)
 * - Generous padding and whitespace
 */

/* ========================================================================== */
/* Global Reset - Enforce Zero Border-Radius Everywhere                       */
/* ========================================================================== */

* {
    border-radius: 0px;
    outline: none;
}

/* ========================================================================== */
/* Base Window Styling                                                        */
/* ========================================================================== */

QMainWindow {
    background: qlineargradient(
        x1: 0,
        y1: 0,
        x2: 0,
        y2: 1,
        stop: 0 $BACKGROUND_PRIMARY,
        stop: 1 $BACKGROUND_WINDOW_GRADIENT_END
    );
}

QWidget {
    background-color: $BACKGROUND_PRIMARY;
    color: $TEXT_PRIMARY;
    font-size: 12px;
}

QWidget#centralWidget {
    background: qlineargradient(
        x1: 0,
        y1: 0,
        x2: 0,
        y2: 1,
        stop: 0 $BACKGROUND_PRIMARY,
        stop: 1 $BACKGROUND_WINDOW_GRADIENT_END
    );
}

QWidget#sidebar {
    background: qlineargradient(
        x1: 0,
        y1: 0,
        x2: 0,
        y2: 1,
        stop: 0 $BACKGROUND_PRIMARY,
        stop: 1 $BACKGROUND_SIDEBAR_GRADIENT_END
    );
}

QWidget#contentArea {
    background: qlineargradient(
        x1: 0,
        y1: 0,
        x2: 0,
        y2: 1,
        stop: 0 $BACKGROUND_PRIMARY,
        stop: 1 $BACKGROUND_CONTENT_GRADIENT_END
    );
}

/* ========================================================================== */
/* Buttons - Primary (Black Background, White Text)                           */
/* ========================================================================== */

QPushButton#primary {
    background-color: $ACCENT_PRIMARY;
    color: $TEXT_INVERTED;
    border: none;
    padding: 8px 16px;
    font-size: 12px;
    font-weight: 600;
    min-height: 32px;
}

QPushButton#primary:hover {
    background-color: $ACCENT_PRIMARY_HOVER;
}

QPushButton#primary:pressed {
    background-color: $ACCENT_PRIMARY_PRESSED;
}

QPushButton#primary:disabled {
    background-color: $BACKGROUND_SUBTLE;
    color: $TEXT_SECONDARY;
}

/* ========================================================================== */
/* Buttons - Secondary (White Background, Black Border)                       */
/* ========================================================================== */

QPushButton#secondary {
    background-color: $BACKGROUND_PRIMARY;
    color: $TEXT_PRIMARY;
    border: 1px solid $BORDER_FOCUSED;
    padding: 8px 16px;
    font-size: 12px;
    font-weight: 400;
    min-height: 32px;
}

QPushButton#secondary:hover {
    background-color: $BACKGROUND_HOVER;
}

QPushButton#secondary:pressed {
    background-color: $BACKGROUND_SUBTLE;
}

QPushButton#secondary:disabled {
    border: 1px solid $BORDER_UNFOCUSED;
    color: $TEXT_SECONDARY;
    background-color: $BACKGROUND_PRIMARY;
}

/* ========================================================================== */
/* Buttons - Default (No Specific Object Name)                                */
/* ========================================================================== */

QPushButton {
    background-color: $BACKGROUND_PRIMARY;
    color: $TEXT_PRIMARY;
    border: 1px solid $BORDER_UNFOCUSED;
    padding: 8px 16px;
    font-size: 12px;
    min-height: 32px;
}

QPushButton:hover {
    border: 1px solid $BORDER_FOCUSED;
    background-color: $BACKGROUND_HOVER;
}

QPushButton:pressed {
    background-color: $BACKGROUND_SUBTLE;
}

QPushButton:disabled {
    color: $TEXT_SECONDARY;
    border: 1px solid $BORDER_UNFOCUSED;
}

/* ========================================================================== */
/* Text Inputs - Line Edit, Text Edit, Spin Boxes                             */
/* ========================================================================== */

QLineEdit, QTextEdit, QPlainTextEdit {
    background-color: $BACKGROUND_PRIMARY;
    border: 1px solid $BORDER_UNFOCUSED;
    padding: 6px 12px;
    color: $TEXT_PRIMARY;
    font-size: 12px;
    min-height: 32px;
}

QLineEdit:focus, QTextEdit:focus, QPlainTextEdit:focus {
    border: 1px solid $BORDER_FOCUSED;
}

QLineEdit:disabled, QTextEdit:disabled, QPlainTextEdit:disabled {
    background-color: $BACKGROUND_SUBTLE;
    color: $TEXT_SECONDARY;
}

QSpinBox, QDoubleSpinBox {
    background-color: $BACKGROUND_PRIMARY;
    border: 1px solid $BORDER_UNFOCUSED;
    padding: 6px 12px;
    color: $TEXT_PRIMARY;
    font-size: 12px;
    min-height: 32px;
}

QSpinBox:focus, QDoubleSpinBox:focus {
    border: 1px solid $BORDER_FOCUSED;
}

QSpinBox::up-button, QDoubleSpinBox::up-button,
QSpinBox::down-button, QDoubleSpinBox::down-button {
    border: none;
    background-color: $BACKGROUND_SUBTLE;
    width: 16px;
}

QSpinBox::up-button:hover, QDoubleSpinBox::up-button:hover,
QSpinBox::down-button:hover, QDoubleSpinBox::down-button:hover {
    background-color: $BACKGROUND_HOVER;
}

QSpinBox::up-arrow, QDoubleSpinBox::up-arrow {
    width: 8px;
    height: 8px;
}

QSpinBox::down-arrow, QDoubleSpinBox::down-arrow {
    width: 8px;
    height: 8px;
}

/* ========================================================================== */
/* Sliders - Black Handle, Black Fill                                         */
/* ========================================================================== */

QSlider::groove:horizontal {
    height: 4px;
    background: $BORDER_UNFOCUSED;
    border: none;
}

QSlider::handle:horizontal {
    background: $ACCENT_PRIMARY;
    width: 16px;
    height: 16px;
    margin: -6px 0;
}

QSlider::handle:horizontal:hover {
    background: $ACCENT_PRIMARY_HOVER;
}

QSlider::handle:horizontal:pressed {
    background: $ACCENT_PRIMARY_PRESSED;
}

QSlider::sub-page:horizontal {
    background: $ACCENT_PRIMARY;
}

QSlider::add-page:horizontal {
    background: $BORDER_UNFOCUSED;
}

QSlider::groove:vertical {
    width: 4px;
    background: $BORDER_UNFOCUSED;
    border: none;
}

QSlider::handle:vertical {
    background: $ACCENT_PRIMARY;
    width: 16px;
    height: 16px;
    margin: 0 -6px;
}

QSlider::sub-page:vertical {
    background: $ACCENT_PRIMARY;
}

QSlider::add-page:vertical {
    background: $BORDER_UNFOCUSED;
}

/* ========================================================================== */
/* ComboBox (Dropdown)                                                        */
/* ========================================================================== */

QComboBox {
    background-color: $BACKGROUND_PRIMARY;
    border: 1px solid $BORDER_UNFOCUSED;
    padding: 6px 12px;
    color: $TEXT_PRIMARY;
    font-size: 12px;
    min-height: 32px;
}

QComboBox:focus {
    border: 1px solid $BORDER_FOCUSED;
}

QComboBox:hover {
    border: 1px solid $BORDER_FOCUSED;
}

QComboBox::drop-down {
    border: none;
    width: 20px;
    background: transparent;
}

QComboBox::down-arrow {
    width: 12px;
    height: 12px;
}

QComboBox QAbstractItemView {
    background-color: $BACKGROUND_PRIMARY;
    border: 1px solid $BORDER_FOCUSED;
    color: $TEXT_PRIMARY;
    selection-background-color: $ACCENT_PRIMARY;
    selection-color: $TEXT_INVERTED;
    padding: 4px;
}

QComboBox QAbstractItemView::item {
    padding: 6px 12px;
    min-height: 24px;
}

QComboBox QAbstractItemView::item:hover {
    background-color: $BACKGROUND_HOVER;
}

QComboBox QAbstractItemView::item:selected {
    background-color: $ACCENT_PRIMARY;
    color: $TEXT_INVERTED;
}

/* ========================================================================== */
/* Checkboxes - Square Indicator                                              */
/* ========================================================================== */

QCheckBox {
    spacing: 8px;
    color: $TEXT_PRIMARY;
    font-size: 12px;
}

QCheckBox::indicator {
    width: 16px;
    height: 16px;
    border: 1px solid $BORDER_UNFOCUSED;
    background: $BACKGROUND_PRIMARY;
}

QCheckBox::indicator:hover {
    border: 1px solid $BORDER_FOCUSED;
}

QCheckBox::indicator:checked {
    background: $ACCENT_PRIMARY;
    border: 1px solid $BORDER_FOCUSED;
}

QCheckBox::indicator:checked:hover {
    background: $ACCENT_PRIMARY_HOVER;
}

QCheckBox::indicator:disabled {
    background: $BACKGROUND_SUBTLE;
    border: 1px solid $BORDER_UNFOCUSED;
}

/* ========================================================================== */
/* Radio Buttons - Square Indicator (Monochrome Override)                     */
/* ========================================================================== */

QRadioButton {
    spacing: 8px;
    color: $TEXT_PRIMARY;
    font-size: 12px;
}

QRadioButton::indicator {
    width: 16px;
    height: 16px;
    border: 1px solid $BORDER_UNFOCUSED;
    background: $BACKGROUND_PRIMARY;
    /* Override default circular shape with square */
}

QRadioButton::indicator:hover {
    border: 1px solid $BORDER_FOCUSED;
}

QRadioButton::indicator:checked {
    background: $ACCENT_PRIMARY;
    border: 1px solid $BORDER_FOCUSED;
}

QRadioButton::indicator:checked:hover {
    background: $ACCENT_PRIMARY_HOVER;
}

/* ========================================================================== */
/* Progress Bar - Black Fill, White Background                                */
/* ========================================================================== */

QProgressBar {
    border: 1px solid $BORDER_UNFOCUSED;
    background: $BACKGROUND_PRIMARY;
    height: 20px;
    text-align: center;
    color: $TEXT_PRIMARY;
    font-size: 10px;
}

QProgressBar::chunk {
    background-color: $ACCENT_PRIMARY;
}

/* ========================================================================== */
/* Scroll Areas and Scroll Bars                                               */
/* ========================================================================== */

QScrollArea {
    border: none;
    background: $BACKGROUND_PRIMARY;
}

QScrollBar:vertical {
    width: 12px;
    background: $BACKGROUND_SUBTLE;
    border: none;
}

QScrollBar::handle:vertical {
    background: $ACCENT_PRIMARY;
    min-height: 20px;
}

QScrollBar::handle:vertical:hover {
    background: $ACCENT_PRIMARY_HOVER;
}

QScrollBar::add-line:vertical,
QScrollBar::sub-line:vertical {
    height: 0px;
}

QScrollBar::add-page:vertical,
QScrollBar::sub-page:vertical {
    background: none;
}

QScrollBar:horizontal {
    height: 12px;
    background: $BACKGROUND_SUBTLE;
    border: none;
}

QScrollBar::handle:horizontal {
    background: $ACCENT_PRIMARY;
    min-width: 20px;
}

QScrollBar::handle:horizontal:hover {
    background: $ACCENT_PRIMARY_HOVER;
}

QScrollBar::add-line:horizontal,
QScrollBar::sub-line:horizontal {
    width: 0px;
}

/* ========================================================================== */
/* Group Boxes and Frames                                                     */
/* ========================================================================== */

QGroupBox {
    border: 1px solid $BORDER_UNFOCUSED;
    margin-top: 12px;
    padding-top: 12px;
    font-size: 12px;
    font-weight: 600;
}

QGroupBox::title {
    subcontrol-origin: margin;
    subcontrol-position: top left;
    padding: 0 8px;
    color: $TEXT_PRIMARY;
}

QFrame {
    border: none;
}

QFrame#divider {
    background-color: $BORDER_DIVIDER;
    max-height: 1px;
    min-height: 1px;
}

/* ========================================================================== */
/* Labels                                                                      */
/* ========================================================================== */

QLabel {
    background: transparent;
    color: $TEXT_PRIMARY;
    font-size: 12px;
}

QLabel#heading {
    font-size: 16px;
    font-weight: 600;
    color: $TEXT_PRIMARY;
}

QLabel#subheading {
    font-size: 14px;
    font-weight: 600;
    color: $TEXT_PRIMARY;
}

QLabel#caption {
    font-size: 10px;
    color: $TEXT_SECONDARY;
}

QLabel#error {
    color: $ACCENT_ERROR;
    font-size: 12px;
}

QLabel#success {
    color: $ACCENT_SUCCESS;
    font-size: 12px;
}

/* ========================================================================== */
/* Stats Cards (Custom Object Name)                                           */
/* ========================================================================== */

QFrame#statCard {
    background-color: $BACKGROUND_SUBTLE;
    border: 1px solid $BORDER_UNFOCUSED;
    padding: 16px;
    max-height: 80px;
    min-height: 80px;
}

QLabel#statLabel {
    font-size: 10px;
    color: $TEXT_SECONDARY;
    text-transform: uppercase;
    letter-spacing: 0.04em;
}

QLabel#statValue {
    font-size: 24px;
    font-weight: 600;
    color: $TEXT_PRIMARY;
}

/* ========================================================================== */
/* Detection Cards (Custom Object Name)                                       */
/* ========================================================================== */

QFrame#detectionCard {
    background-color: $BACKGROUND_PRIMARY;
    border: 1px solid $BORDER_UNFOCUSED;
    padding: 16px;
}

/* ========================================================================== */
/* Collapsible Section Header (Custom Object Name)                            */
/* ========================================================================== */

QPushButton#collapseHeader {
    background-color: $BACKGROUND_PRIMARY;
    border: 1px solid $BORDER_UNFOCUSED;
    padding: 12px;
    text-align: left;
    font-size: 12px;
    font-weight: 600;
    color: $TEXT_PRIMARY;
}

QPushButton#collapseHeader:hover {
    background-color: $BACKGROUND_HOVER;
}

QPushButton#collapseHeader:pressed {
    background-color: $BACKGROUND_SUBTLE;
}

/* ========================================================================== */
/* Custom Title Bar (Object Name: customTitleBar)                             */
/* ========================================================================== */

QWidget#customTitleBar {
    background-color: $BACKGROUND_PRIMARY;
    border-bottom: 1px solid $BORDER_UNFOCUSED;
    min-height: 40px;
    max-height: 40px;
}

QPushButton[titleBarControl="true"] {
    border: 1px solid transparent;
    padding: 0;
    min-width: 12px;
    max-width: 12px;
    min-height: 12px;
    max-height: 12px;
    border-radius: 6px;
}

QPushButton[titleBarControl="true"]:hover {
    border-color: $BORDER_FOCUSED;
}

QPushButton[titleBarRole="close"] {
    background-color: $TITLEBAR_CLOSE_BG;
    border: 1px solid $TITLEBAR_CLOSE_BORDER;
}

QPushButton[titleBarRole="minimize"] {
    background-color: $TITLEBAR_MINIMIZE_BG;
    border: 1px solid $TITLEBAR_MINIMIZE_BORDER;
}

QPushButton[titleBarRole="maximize"] {
    background-color: $TITLEBAR_MAXIMIZE_BG;
    border: 1px solid $TITLEBAR_MAXIMIZE_BORDER;
}

QPushButton[titleBarRole="close"]:hover {
    background-color: $TITLEBAR_CLOSE_HOVER_BG;
}

QPushButton[titleBarRole="minimize"]:hover {
    background-color: $TITLEBAR_MINIMIZE_HOVER_BG;
}

QPushButton[titleBarRole="maximize"]:hover {
    background-color: $TITLEBAR_MAXIMIZE_HOVER_BG;
}

QLabel#titleBarLabel {
    font-size: 12px;
    font-weight: 600;
    color: $TEXT_PRIMARY;
    padding-left: 8px;
}

QTabBar#titleBarTabs {
    background-color: $BACKGROUND_PRIMARY;
}

QTabBar#titleBarTabs::tab {
    background-color: $BACKGROUND_SUBTLE;
    border: 1px solid $BORDER_UNFOCUSED;
    padding: 6px 12px;
    margin-right: 0px;
    color: $TEXT_PRIMARY;
    font-size: 12px;
}

QTabBar#titleBarTabs::tab:selected {
    background: $BACKGROUND_PRIMARY;
    border-bottom: 2px solid $ACCENT_PRIMARY;
}

QTabBar#titleBarTabs::tab:hover {
    background: $BACKGROUND_HOVER;
}

/* ========================================================================== */
/* Tab Widgets                                                                 */
/* ========================================================================== */

QTabWidget::pane {
    border: 1px solid $BORDER_UNFOCUSED;
    background: $BACKGROUND_PRIMARY;
}

QTabBar::tab {
    background: $BACKGROUND_SUBTLE;
    border: 1px solid $BORDER_UNFOCUSED;
    padding: 8px 16px;
    color: $TEXT_PRIMARY;
    font-size: 12px;
}

QTabBar::tab:selected {
    background: $BACKGROUND_PRIMARY;
    border-bottom: 1px solid $BACKGROUND_PRIMARY;
}

QTabBar::tab:hover {
    background: $BACKGROUND_HOVER;
}

/* ========================================================================== */
/* Tooltips                                                                    */
/* ========================================================================== */

QToolTip {
    background-color: $TOOLTIP_BACKGROUND;
    color: $TOOLTIP_TEXT;
    border: none;
    padding: 6px 12px;
    font-size: 11px;
}

/* ========================================================================== */
/* Menu and Menu Bar                                                           */
/* ========================================================================== */

QMenuBar {
    background-color: $BACKGROUND_PRIMARY;
    border-bottom: 1px solid $BORDER_UNFOCUSED;
    color: $TEXT_PRIMARY;
}

QMenuBar::item {
    padding: 8px 16px;
    background: transparent;
}

QMenuBar::item:selected {
    background: $BACKGROUND_HOVER;
}

QMenu {
    background-color: $BACKGROUND_PRIMARY;
    border: 1px solid $BORDER_FOCUSED;
    padding: 4px;
}

QMenu::item {
    padding: 8px 24px;
    color: $TEXT_PRIMARY;
}

QMenu::item:selected {
    background-color: $ACCENT_PRIMARY;
    color: $TEXT_INVERTED;
}

QMenu::separator {
    height: 1px;
    background: $BORDER_DIVIDER;
    margin: 4px 0;
}

/* ========================================================================== */
/* List and Tree Widgets                                                       */
/* ========================================================================== */

QListWidget, QTreeWidget {
    background-color: $BACKGROUND_PRIMARY;
    border: 1px solid $BORDER_UNFOCUSED;
    color: $TEXT_PRIMARY;
}

QListWidget::item, QTreeWidget::item {
    padding: 6px 12px;
}

QListWidget::item:selected, QTreeWidget::item:selected {
    background-color: $ACCENT_PRIMARY;
    color: $TEXT_INVERTED;
}

QListWidget::item:hover, QTreeWidget::item:hover {
    background-color: $BACKGROUND_HOVER;
}

/* ========================================================================== */
/* Splitter                                                                    */
/* ========================================================================== */

QSplitter::handle {
    background-color: $BORDER_DIVIDER;
}

QSplitter::handle:horizontal {
    width: 1px;
}

QSplitter::handle:vertical {
    height: 1px;
}

QSplitter::handle:hover {
    background-color: $BORDER_FOCUSED;
}

/* ========================================================================== */
/* Status Bar                                                                  */
/* ========================================================================== */

QStatusBar {
    background: $BACKGROUND_PRIMARY;
    border-top: 1px solid $BORDER_UNFOCUSED;
    color: $TEXT_PRIMARY;
}

/* ========================================================================== */
/* Dialog Windows                                                              */
/* ========================================================================== */

QDialog {
    background-color: $BACKGROUND_PRIMARY;
}

QMessageBox {
    background-color: $BACKGROUND_PRIMARY;
}

QMessageBox QPushButton {
    min-width: 80px;
}
